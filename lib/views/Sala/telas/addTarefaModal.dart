import 'package:flutter/material.dart';

class AddTarefaModal extends StatelessWidget {
  const AddTarefaModal({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      
    );
  }
}